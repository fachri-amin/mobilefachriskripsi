import { transmisiOptions, merkOptions, jenisOptions } from './prediction'

const bgColor = {
	primary: '#FF8C32',
	secondary: '#06113C',
	grey1: '#dddddd',
	grey2: '#eeeeee',
}

export { bgColor, transmisiOptions, merkOptions, jenisOptions }
